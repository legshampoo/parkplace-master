// import React from 'react';
//
// import TextLink from './TextLink';
//
// //pass the className as a prop, based on what is selected
// const RenderAssets = ({data, handleClick, activeButton, mediaType}) =>
//   <div className='media-container-inner-wrapper'>
//     {data.map((media, index) => (
//       <TextLink
//         handleClick={handleClick.bind(this, media, mediaType)}
//         mediaType={mediaType}
//         activeButton={activeButton}
//         id={media.name}
//         key={index}
//         path={media.pan}
//         displayText={media.name}
//         styleClass={media.name === activeButton ? 'media-asset-button-active' : 'media-asset-button' }
//         />)
//     )}
//   </div>
//
//
//
// export default RenderAssets
